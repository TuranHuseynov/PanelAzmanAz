@model IEnumerable<PanelWork.Models.SifarisNovu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.sifaris_type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sifaris_type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.sifaris_type_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.sifaris_type_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.sifaris_type_id })
        </td>
    </tr>
}

</table>
